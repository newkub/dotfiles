1. git init
2. สร้างให้มีทั้งหมด 4 branch ได้แก่ 
- main
- dev
- preview
- experiment
3. เพิ่ม README.md, .gitignore (ถ้ายังไม่มี)
4. git commit -m "chore(repo): initial commit"
5. สร้าง gh repo create <my-repo> --public --source=. --remote=origin --push
