[tasks.update-all]
description = "update all tools"
shell = "pwsh -NoProfile -Command"
run = '''
$ErrorActionPreference = "Continue"
$ProgressPreference = "SilentlyContinue"
$script:failed = 0

function Strip-Ansi ($text) {
    $text -replace '\x1b\[[0-9;]*m', ''
}

function Step ($name, $scriptblock) {
    Write-Host "`n[update-all] $name" -ForegroundColor Cyan
    try {
        $lastExit = 0
        $output = & $scriptblock 2>&1
        $lastExit = $LASTEXITCODE
        if ($lastExit -ne 0) { throw "exit code $lastExit" }
        $output | ForEach-Object { Write-Host "  $_" }
        Write-Host "  OK" -ForegroundColor Green
    } catch {
        Write-Host "  FAIL: $_" -ForegroundColor Red
        $script:failed++
    }
}

function Test-Admin {
    $p = [Security.Principal.WindowsPrincipal]::new([Security.Principal.WindowsIdentity]::GetCurrent())
    $p.IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
}

Step "winget sources" {
    $ok = $true
    winget source update --name msstore; if ($LASTEXITCODE -ne 0) { $ok = $false }
    winget source update --name winget; if ($LASTEXITCODE -ne 0) { $ok = $false }
    winget source update --name winget-font; if ($LASTEXITCODE -ne 0) { $ok = $false }
    if (-not $ok) { exit 1 }
}

Step "scoop" {
    scoop update | ForEach-Object { Write-Host "  $_" }
    if ($LASTEXITCODE -ne 0) { throw }

    scoop hold mise | ForEach-Object { Write-Host "  $_" }

    $outdated = (scoop status 6>&1 | Out-String -Stream) |
        ForEach-Object { Strip-Ansi $_ } |
        Select-String '^(?!Name|----|\s*$)([^\s]+)' |
        ForEach-Object { $_.Matches.Groups[1].Value } |
        Sort-Object -Unique

    foreach ($pkg in $outdated) {
        if ($pkg -eq 'mise') {
            Write-Host "  skip $pkg (held/self-managed)" -ForegroundColor Yellow
            continue
        }
        Write-Host "  update $pkg"
        try {
            $out = scoop update $pkg 2>&1
            $out | ForEach-Object { Write-Host "    $_" }
            if ($LASTEXITCODE -ne 0) { throw }
            Write-Host "    OK" -ForegroundColor Green
        } catch {
            Write-Host "    FAIL: $_" -ForegroundColor Red
            $script:failed++
        }
    }
}

Step "winget" {
    $wingetSkip = @(
        'BandicamCompany.Bandicam',
        'BandicamCompany.Bandicut',
        'Chocolatey.Chocolatey',
        'Microsoft.Edge'
    )
    foreach ($id in $wingetSkip) {
        winget pin add --id $id 2>&1 | ForEach-Object { Write-Host "  pin: $_" }
        $null = $LASTEXITCODE
    }
    winget upgrade --all --accept-source-agreements --accept-package-agreements
}

Step "bun" { bun update -g }
Step "npm" { npm update -g }
Step "pnpm" { pnpm update -g }

Step "pip" {
    python -m pip install --upgrade pip
    if ($LASTEXITCODE -ne 0) { throw }
    python -m pip_review --auto
}

Step "uv" { uv tool upgrade --all }

Step "pipx" {
    if (Get-Command pipx -ErrorAction SilentlyContinue) {
        pipx upgrade-all
    } else {
        Write-Host "  pipx not installed, skipping" -ForegroundColor Yellow
    }
}

Step "choco" {
    if (Test-Admin) {
        choco upgrade all -y
    } else {
        Write-Host "  not admin, skipping choco" -ForegroundColor Yellow
    }
}

Step "cargo" {
    if (-not (Get-Command cargo-install-update -ErrorAction SilentlyContinue)) {
        if (Get-Command cargo-binstall -ErrorAction SilentlyContinue) {
            cargo-binstall cargo-update -y
        } else {
            cargo install cargo-update
        }
    }
    cargo install-update -a
}

Step "wsl" {
    if (Test-Admin -and (Get-Command wsl -ErrorAction SilentlyContinue)) {
        wsl --update
    } else {
        Write-Host "  skip wsl (not admin or not installed)" -ForegroundColor Yellow
    }
}

Step "mise tools" { mise upgrade }
Step "mise prune" { mise prune --yes }
Step "mise reshim" { mise reshim }
Step "mise self-update" { mise self-update -y }

Step "rustup" { rustup update stable }
Step "gh extensions" { gh extension upgrade --all }
Step "chezmoi" { mise run chezmoi-update }
Step "skills" {
    skills update
    skills update -g
}

Write-Host "`n[update-all] completed with $failed failure(s)" -ForegroundColor $(if ($failed -eq 0) { 'Green' } else { 'Red' })
exit $failed
'''

[tasks.cleanup]
description = "cleanup caches"
shell = "pwsh -Command"
run = "scoop cleanup * && scoop cache rm * && Remove-Item -Recurse -Force $env:USERPROFILE/.bun/install/cache/* -ErrorAction SilentlyContinue ; npm cache clean --force 2>$null ; $null = $LASTEXITCODE ; pnpm store prune 2>$null ; $null = $LASTEXITCODE ; yarn cache clean 2>$null ; $null = $LASTEXITCODE ; uv cache clean 2>$null ; $null = $LASTEXITCODE ; uv cache prune 2>$null ; $null = $LASTEXITCODE ; python -m pip cache purge 2>$null ; $null = $LASTEXITCODE ; bun pm cache rm 2>$null ; $null = $LASTEXITCODE ; mise cache clear 2>$null ; $null = $LASTEXITCODE ; Clear-RecycleBin -Force -ErrorAction SilentlyContinue ; Remove-Item -Path $env:TEMP/* -Recurse -Force -ErrorAction SilentlyContinue ; Remove-Item -Path $env:LOCALAPPDATA/mise/downloads/* -Recurse -Force -ErrorAction SilentlyContinue ; Remove-Item -Path $env:USERPROFILE/.cache/pip/* -Recurse -Force -ErrorAction SilentlyContinue ; Remove-Item -Path $env:LOCALAPPDATA/Microsoft/Windows/WER/* -Recurse -Force -ErrorAction SilentlyContinue ; Remove-Item -Path $env:LOCALAPPDATA/CrashDumps/* -Recurse -Force -ErrorAction SilentlyContinue ; npkill --delete-all -y 2>$null ; cargo-clean-all 2>$null ; podman system prune -af 2>$null ; mise prune --yes ; git gc --auto 2>$null ; if ($env:SCCACHE_DIR) { Remove-Item -Recurse -Force $env:SCCACHE_DIR -ErrorAction SilentlyContinue } ; exit 0"

[tasks.chezmoi-update]
description = "sync local dotfiles"
dir = "C:/Users/Veerapong/.local/share/chezmoi"
shell = "pwsh -Command"
run = "chezmoi re-add"

[tasks.cargo-clean-all]
description = "Clean all Rust projects recursively"
shell = "pwsh -Command"
run_windows = '''powershell -NoProfile -Command "Get-ChildItem -Recurse -Filter Cargo.toml | ForEach-Object { `$dir = `$_.DirectoryName; Write-Host \\"Cleaning `$dir\\"; Push-Location `$dir; cargo clean; Pop-Location }"'''

[tasks.program-list]
description = "list installed programs"
shell = "pwsh -Command"
run = '& { function Show-Section ($title, $script, $limit = 0) { Write-Host "`n=== $title ===" -ForegroundColor Cyan; try { $out = & $script 2>$null } catch { $out = $null }; if (-not $out) { Write-Host "  no output / not installed" -ForegroundColor Yellow } else { $lines = @($out); $total = $lines.Count; if ($limit -gt 0 -and $total -gt $limit) { $lines | Select-Object -First $limit | Out-Host; Write-Host "  ... $total lines total, showing first $limit" -ForegroundColor Gray } else { $lines | Out-Host; Write-Host "  $total lines" -ForegroundColor Gray } } }; Show-Section "Scoop" { scoop list }; Show-Section "Mise" { mise list }; Show-Section "Bun" { bun pm ls -g }; Show-Section "npm" { npm list -g --depth=0 }; Show-Section "pnpm" { pnpm list -g }; Show-Section "uv" { uv tool list }; Show-Section "pip" { python -m pip list }; Show-Section "winget" { winget list } 50; Show-Section "gh" { gh extension list } }'

[env]
OPENAI_API_KEY = "${OPENAI_API_KEY:-}"
GEMINI_API_KEY = "${GEMINI_API_KEY:-}"
CLAUDE_API_KEY = "${CLAUDE_API_KEY:-}"
OPENROUTER_API_KEY = "${OPENROUTER_API_KEY:-}"
DEEPSEEK_API_KEY = "${DEEPSEEK_API_KEY:-}"
XAI_API_KEY = "${XAI_API_KEY:-}"
MINIMAX_API_KEY = "${MINIMAX_API_KEY:-}"
SCCACHE_DIR = "D:\\.cache\\sccache"
MISE_INSTALLS_DIR = "D:\\mise"

[tools]
"github:kristoferlund/ostt" = "latest"
"github:stakpak/agent" = "latest"
watchexec = "latest"
cargo-binstall = "latest"
docker-compose = "latest"
node = "latest"
rush = "latest"
bat = "latest"
bottom = "latest"
chezmoi = "latest"
dua = "latest"
duf = "latest"
fd = "latest"
fzf = "latest"
gitleaks = "latest"
gitui = "latest"
glow = "latest"
hyperfine = "latest"
uv = "latest"
yazi = "latest"
ttyd = "latest"
edit = "latest"
task = "latest"
television = "latest"
rg = "latest"
zig = "latest"
rust = "latest"
act = "latest"
gh = "latest"
worktrunk = "latest"
oxker = "latest"
zoxide = "latest"
atuin = "latest"
helix = "latest"
python = "latest"
jq = "latest"
yq = "latest"
helm = "latest"
k9s = "latest"
kubectl = "latest"
terraform = "latest"
gitu = "latest"
git-lfs = "latest"
dotenv-linter = "latest"
sccache = "latest"
ast-grep = "latest"
starship = "latest"
viteplus = "latest"
amp = "latest"
oxfmt = "latest"
cowsay = "latest"
"npm:@antfu/ni" = "latest"
"npm:@modelcontextprotocol/sdk" = "latest"
"npm:@moonrepo/cli" = "latest"
"npm:@openai/codex" = "latest"
"npm:@shikijs/cli" = "latest"
"npm:@tanstack/intent" = "latest"
"npm:@typescript/native-preview" = "latest"
"npm:@vscode/vsce" = "latest"
"npm:actions-up" = "latest"
"npm:agent-browser" = "latest"
"npm:changelogen" = "latest"
"npm:chrome-devtools-mcp" = "latest"
"npm:giget" = "latest"
"npm:gifify" = "latest"
"npm:gitify" = "latest"
"npm:jscpd" = "latest"
"npm:oxlint" = "latest"
"npm:playwright" = "latest"
"npm:rimraf" = "latest"
"npm:playwriter" = "latest"
"npm:rumdl" = "latest"
"npm:skills" = "latest"
"npm:tauri" = "latest"
"npm:tree-sitter-cli" = "latest"
"npm:typescript" = "latest"
"npm:unocss-language-server" = "latest"
"npm:vitest" = "latest"
"npm:vercel" = "latest"
"npm:vue-tsc" = "latest"
"npm:@schpet/linear-cli" = "latest"
"aqua:eza-community/eza" = "latest"
"cargo:rmz" = "latest"
"cargo:open" = "latest"
"cargo:git-x" = "latest"
"cargo:xh" = "latest"
"cargo:ast-grep-mcp" = "latest"
"cargo:rust-mcp-filesystem" = "latest"
"cargo:elio" = "latest"
"cargo:bacon" = "latest"
"cargo:slumber" = "latest"
"cargo:rainfrog" = "latest"
"cargo:crw-server" = "latest"
"cargo:rmux" = "latest"
"cargo:clean-dev-dirs" = "latest"
"cargo:selene" = "latest"
"cargo:cargo-sweep" = "latest"
"cargo:cargo-show-asm" = "latest"
"cargo:procs" = "latest"
"cargo:node_module-nuke" = "latest"
"cargo:cargo-clean-all" = "latest"
"cargo:git-graph" = "latest"
"cargo:rloc" = "latest"
"cargo:loc" = "latest"
usage = "latest"
"npm:crw-mcp" = "latest"
"npm:aicommits" = "latest"
"github:us/crw" = "latest"
"npm:gleanup" = "latest"
aube = "latest"
"npm:foam-cli" = "latest"
"npm:taze" = "latest"
"npm:pake-cli" = "latest"
ffmpeg = "latest"
bun = "latest"
opencode = "latest"
podman-tui = "latest"
podman = { version = "latest", rename_exe = "podman-remote" }
"github:dedene/raindrop-cli" = "latest"
pnpm = "latest"
"npm:ctx7" = "latest"
lazygit = "latest"
"npm:@mermaid-js/mermaid-cli" = "latest"
"npm:@biomejs/biome" = "latest"
"npm:@native-sdk/cli" = "latest"
"aqua:wasm-bindgen/wasm-pack" = "latest"
imagemagick = "latest"
"npm:druk" = "latest"
"npm:npkill" = "latest"
"npm:git-split-diffs" = "latest"
hk = "latest"
"aqua:moonrepo/moon" = "2.5.2"
"npm:wrangler" = "latest"
# powershell-core = "latest"  # installed via scoop to avoid pwsh shim loading full mise env

[settings]
env_shell_expand = true
windows_shim_mode = "exe"
experimental = true
minimum_release_age = "0"

[settings.npm]
bun = true
