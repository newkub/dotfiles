[tasks.update]
description = "update scoop, winget, bun, mise"
run = "mise cleanup && scoop update * && winget upgrade --all && bun update -g && mise upgrade && mise self-update -y && rustup update stable"


# chezmoi tasks
[tasks.chezmoi-update-and-push]
description = "chezmoi update && push"
run = "chezmoi re-add && git commit -m update && git push -u origin main"



[tasks.cleanup]
description = "cleanup"
run = "scoop cleanup * && mise prune"


[tasks.commit]
description = "commit"
run = "git add . && czg ai" 


[tasks.start-podman]
description = "start podman"
run = "podman machine start" 






#[tasks.commitfile]
#description = "commit file"
#run = "!f() { git add \"$1\" && czg ai }; f"  

[tasks.copy-path]
description = "copy current path"
run = "(Get-Location).Path | Set-Clipboard"


[env]
OPENAI_API_KEY = "${env:OPENAI_API_KEY}"
